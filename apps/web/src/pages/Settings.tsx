export function Settings() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-white">Settings</h1>
      <p className="mt-2 text-white/60">
        Nothing configurable here yet — voice/style presets and provider settings land once the
        video, voice, and LLM engines are wired up (all currently gated behind Nobert's approval).
      </p>
    </div>
  );
}
